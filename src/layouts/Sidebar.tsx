import XNavigation from "../components/XNavigation";


// This component is not needed
// Still keeping it, in case if we need to add anything else in the sidebar in future 
function Sidebar() {

  return (
    <XNavigation />
  );
}

export default Sidebar;